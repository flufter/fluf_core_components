name: core_app_icon
description: A Flutter component that manages app icons across different platforms. This brick generates the necessary configuration and assets structure for handling multiple app icons, supporting iOS and Android platforms with environment-specific variations (dev, staging, test, prod).

# The following defines the brick repository url.
# Uncomment and update the following line before publishing the brick.
# repository: https://github.com/my_org/my_repo

# The following defines the version and build number for your brick.
# A version number is three numbers separated by dots, like 1.2.34
# followed by an optional build number (separated by a +).
version: 0.0.3-dev

# The following defines the environment for the current brick.
# It includes the version of mason that the brick requires.
environment:
  mason: ">=0.1.0 <0.2.0"

# Variables specify dynamic values that your brick depends on.
# Zero or more variables can be specified for a given brick.
# Each variable has:
#  * a type (string, number, boolean, enum, or array)
#  * an optional short description
#  * an optional default value
#  * an optional list of default values (array only)
#  * an optional prompt phrase used when asking for the variable
#  * a list of values (enums only)

vars:
  project_name:
    type: string
    description: The name of the project
    default: my_project_name
    prompt: What is the name of the project?
  environments:
    type: array
    description: The environments to create
    values:
      - dev
      - stag
      - testing
      - prod
    prompt: Which environments do you want to create?